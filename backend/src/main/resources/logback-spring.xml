<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Property for log directory -->
    <property name="LOG_DIR" value="${LOG_DIR:-./logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-medmentor-rag}"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{requestId}] - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{requestId}] - %msg%n%ex</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep 30 days worth of history -->
            <maxHistory>30</maxHistory>
            <!-- Total size cap of 1GB -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Logger for application -->
    <logger name="ru.medmentor" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!--
        Audit loggers for analyzing exactly what flows through the RAG/LLM pipeline.
        Default OFF (INFO) so production logs stay clean. Toggle to DEBUG to see:
          - input    : raw user inputs (case selection, doctor messages, diagnoses)
          - rag      : RAG query text + every retrieved chunk verbatim
          - prompt   : the variable parts of the LLM prompt (case data, RAG context,
                       conversation, latest user message). Fixed system templates
                       are intentionally omitted from the log to keep it readable.
          - response : the model's full reply, emitted once when streaming completes
                       (no per-chunk noise). Includes score-review JSON.
        Either toggle a single family or set the parent `ru.medmentor.audit` to
        DEBUG to enable all four at once.

        Override via env var: MEDMENTOR_AUDIT_LOG_LEVEL=DEBUG
        Or per-family from application.properties:
          logging.level.ru.medmentor.audit.rag=DEBUG
    -->
    <logger name="ru.medmentor.audit" level="${MEDMENTOR_AUDIT_LOG_LEVEL:-INFO}" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Logger for Spring Framework -->
    <logger name="org.springframework" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Logger for SQL queries (development) -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Logger for SQL parameters (development) -->
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
